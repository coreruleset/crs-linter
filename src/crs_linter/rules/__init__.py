# import all checks
from crs_linter.lint_problem import LintProblem

from . import (
    approved_tags,
    capture,
    crs_tag,
    ctl_audit_log,
    deprecated,
    duplicated,
    duplicated_ids,
    ignore_case,
    indentation,
    lowercase_ignorecase,
    ordered_actions,
    pl_consistency,
    pl_tags,
    rule_tests,
    variables_usage,
    version
)
