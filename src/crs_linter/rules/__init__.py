# import all checks

from . import (
    approved_tags,
    capture,
    crs_tag,
    deprecated,
    duplicated,
    ignore_case,
    indentation,
    lowercase_ignorecase,
    ordered_actions,
    pl_consistency,
    pl_tags,
    variables_usage,
    version
)
